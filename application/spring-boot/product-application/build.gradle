plugins {
    id 'org.springframework.boot' version "${springBootVersion}"
    id 'maven-publish'
}

def modules = loadStackConfig()

dependencies {
    api project(':product-port-inbound')
    api project(':product-port-outbound')

    api project(':actuator')
    switch (modules['api-server']) {
        case 'rest':
            api project(':product-api-server-rest')
            break
        case 'grpc':
            api project(':product-api-server-grpc-netdevh')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['api-server']}")
    }
    switch (modules['cache']) {
        case 'caffeine':
            api project(':cache-caffeine')
            break
        case 'ehcache':
            api project(':cache-ehcache')
            break
        case 'redis':
            api project(':cache-redis')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['cache']}")
    }
    api project(':config')
    switch (modules['csv']) {
        case 'opencsv':
            api project(':product-csv-opencsv')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['csv']}")
    }
    switch (modules['datasource']) {
        case 'mysql':
            api project(':product-datasource-mysql')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['datasource']}")
    }
    switch (modules['discovery-client']) {
        case 'none':
            break
        case 'eureka':
            api project(':discovery-client-eureka')
            break
        case 'kubernetes':
            api project(':discovery-client-kubernetes')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['discovery-client']}")
    }
    switch (modules['event']) {
        case 'kafka':
            api project(':product-event-kafka')
            break
        case 'sns':
            api project(':product-event-sns')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['event']}")
    }
    switch (modules['excel']) {
        case 'easyexcel':
            api project(':product-excel-easyexcel')
            break
        case 'poi':
            api project(':product-excel-poi')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['excel']}")
    }
    switch (modules['logging']) {
        case 'logback':
            api project(':logging-logback')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['logging']}")
    }
    switch (modules['persistence']) {
        case 'jpa':
            api project(':product-persistence-jpa')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['persistence']}")
    }
    switch (modules['storage']) {
        case 'filesystem':
            api project(':storage-filesystem')
            break
        case 's3s':
            api project(':storage-s3')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['storage']}")
    }
    switch (modules['tracing']) {
        case 'none':
            break
        case 'zipkin':
            api project(':tracing-zipkin')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${modules['tracing']}")
    }
    api project(':use-case-application')
}

jar {
    enabled = false
}

publishing {
    publications {
        publication(MavenPublication) {
            artifact bootJar
            from components.java
        }
    }
}
