spring:
  kafka:
    consumer:
      group-id: ${spring.application.name}
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

#app:
#  event:
#    consumer:
#      kafka:
#        topics:
#          - topic1
#          - topic2
#          - topic3
