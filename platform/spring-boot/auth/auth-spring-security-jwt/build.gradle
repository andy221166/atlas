dependencies {
    // Lib
    api 'org.springframework.boot:spring-boot-starter-security'

    // Adapter
    api project(':adapter-spring-boot:api-server-rest')
    api project(':adapter-spring-boot:jwt-auth0')

    // Port
    api project(':user-port-outbound')
}
