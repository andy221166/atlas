dependencies {
    // Lib
    api 'org.springframework.boot:spring-boot-starter-web'
    api 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.6'

    // Commons
    api project(':commons')

    // Adapter
    api project(':adapter-spring-boot:config')
    api project(':adapter-spring-boot:i18n')
    api project(':adapter-spring-boot:json')
}
