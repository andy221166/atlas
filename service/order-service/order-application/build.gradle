plugins {
    id 'maven-publish'
    id 'org.springframework.boot' version '3.4.0'
}

def config = rootProject.ext.loadProfileConfig()

dependencies {
    api project(':order-contract')

    switch (config['api-server']) {
        case 'rest':
            api project(':rest-server-order')
            break
        case 'grpc':
            api project(':grpc-server-order')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['api-server']}")
    }

    switch (config['api-client']) {
        case 'rest-apachehttpclient':
            api project(':rest-client-apachehttpclient')
            break
        case 'rest-feign':
            api project(':rest-client-feign')
            break
        case 'rest-resttemplate':
            api project(':rest-client-resttemplate')
            break
        case 'grpc':
            api project(':grpc-client')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['api-client']}")
    }

    switch (config['csv']) {
        case 'opencsv':
            api project(':csv-opencsv')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['csv']}")
    }

    switch (config['discovery-client']) {
        case 'none':
            break
        case 'eureka':
            api project(':eureka-client')
            break
        case 'kubernetes':
            api project(':kubernetes-client')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['discovery-client']}")
    }

    switch (config['event']) {
        case 'kafka':
            api project(':event-kafka')
            break
        case 'rabbitmq':
            api project(':event-rabbitmq')
            break
        case 'sns':
            api project(':event-sns')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['event']}")
    }

    switch (config['excel']) {
        case 'easyexcel':
            api project(':excel-easyexcel')
            break
        case 'poi':
            api project(':excel-poi')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['excel']}")
    }

    switch (config['logging']) {
        case 'logback':
            api project(':logging-logback')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['logging']}")
    }

    api project(':monitoring')

    switch (config['persistence']) {
        case 'jdbc':
            api project(':jdbc-order')
            break
        case 'jpa':
            api project(':jpa-order')
            break
        case 'mybatis':
            api project(':mybatis-order')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['persistence']}")
    }

    switch (config['tracing']) {
        case 'none':
            break
        case 'zipkin':
            api project(':tracing-zipkin')
            break
        default:
            throw new IllegalArgumentException("Unsupported ${config['tracing']}")
    }
}

jar {
    enabled = false
}

publishing {
    publications {
        publication(MavenPublication) {
            artifact bootJar
            from components.java
        }
    }
}
