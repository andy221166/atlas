dependencies {
    // Lib
    api 'org.springframework.boot:spring-boot-starter-security'

    // Domain
    api project(':user-domain')

    // Infrastructure
    api project(':infrastructure-spring-boot:api-server-rest-core')
    api project(':infrastructure-spring-boot:jwt-auth0')
}
